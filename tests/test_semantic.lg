int a, **b, c[1][2], **d[1][4];

struct s {
  int a;
  int b, **c;
  curve d, *e[2];
};

char** df;
curve **f[2], g, *h;

curve crv(x, y, z), *crvp;

func int** main(int *a, curve b, curve **c[1][2]){
    int a;
    int *b[2];
    int c, *d, e[2], *f[2], *g[4][3];
    curve *h, **i[2][3];
    curve f(x, y), g(y, z), *h;
    complex c;
    vector<int, char, curve**, void> a;
}

int** a;

func int as(int a, int b, int c);
