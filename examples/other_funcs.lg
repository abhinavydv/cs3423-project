import "funcs.lg";


func int main(){
    rational <int, int> c = {1, 2};
    curve y (x, a, b) = a * sin(x) + b * cos(x) ^ 2 + a * b;
    curve w (x) = x^4 + 3*x^2 - 2*x;

    curve z = input_poly(5) / w;

    curve z2 = z * y;

    curve y1 = D[y, x];

    z2 = D[z2, x];
    curve z3 = D[y*z2, x];

    print_curve(z3);

    return 0;
}
